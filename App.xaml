<?xml version="1.0" encoding="UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HeatInputCalculator.App">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Estilos -->
            <Color x:Key="BackgroundLight">#FFFFFF</Color>
            <Color x:Key="TextLight">#222222</Color>
            <Color x:Key="SecondaryTextLight">#555555</Color>
            <Color x:Key="CardBackgroundLight">#F5F5F5</Color>
            <Color x:Key="BackgroundDark">#121212</Color>
            <Color x:Key="TextDark">#FFFFFF</Color>
            <Color x:Key="SecondaryTextDark">#CCCCCC</Color>
            <Color x:Key="CardBackgroundDark">#1E1E1E</Color>
            <Style TargetType="ContentPage">
                <Setter Property="BackgroundColor"
                        Value="{AppThemeBinding Light={StaticResource BackgroundLight},
                                                Dark={StaticResource BackgroundDark}}" />
            </Style>

            <Style TargetType="Label" x:Key="TitleLabel">
                <Setter Property="FontSize" Value="28" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource TextLight},
                                                                       Dark={StaticResource TextDark}}" />
            </Style>

            <Style TargetType="Label" x:Key="BodyLabel">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource SecondaryTextLight},
                                                                       Dark={StaticResource SecondaryTextDark}}" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
