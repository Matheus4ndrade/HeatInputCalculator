<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HeatInputCalculator.MainPage">

    <Grid RowDefinitions="*,Auto">

        <VerticalStackLayout Padding="30"
                             Spacing="25"
                             VerticalOptions="Start"
                             HorizontalOptions="Fill">

            <Grid ColumnDefinitions="*,Auto" VerticalOptions="Center" HorizontalOptions="Fill">
                <!-- centralizando -->
                <Label Grid.Column="0"
                       Text="Heat Input"
                       Style="{StaticResource TitleLabel}"
                       VerticalOptions="Center"
                       HorizontalOptions="Center"/>
                <Frame Grid.Column="1"
                       x:Name="toggleFrame"
                       WidthRequest="60" HeightRequest="32" Padding="2"
                       CornerRadius="16"
                       BackgroundColor="{AppThemeBinding Light=#CCCCCC, Dark=#555555}"
                       VerticalOptions="Center"
                       HasShadow="False">

                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnToggleThemeClicked"/>
                    </Frame.GestureRecognizers>

                    <Frame x:Name="toggleThumb"
                           WidthRequest="28" HeightRequest="28"
                           CornerRadius="14"
                           BackgroundColor="White"
                           HorizontalOptions="Start"
                           VerticalOptions="Center"
                           TranslationX="0"/>
                </Frame>
            </Grid>

            <Frame CornerRadius="15"
                   Padding="20"
                   HasShadow="True"
                   BackgroundColor="{AppThemeBinding Light={StaticResource CardBackgroundLight},
                                                     Dark={StaticResource CardBackgroundDark}}">
                <VerticalStackLayout Spacing="20">
                    <Label Text="O Heat Input, ou energia de soldagem, é a quantidade de energia térmica fornecida ao material durante o processo de soldagem, calculada a partir da corrente elétrica, tensão, eficiência do processo e velocidade de soldagem. Entender o heat input é fundamental, pois influencia diretamente a microestrutura do material, a resistência mecânica da junta e a presença de deformações ou fissuras. Um controle adequado evita defeitos, reduz retrabalho e garante a qualidade do produto final.&#x0a;&#x0a;Essa é uma ferramenta prática que permite calcular rapidamente a energia aplicada em cada solda. Com ele, é possível planejar processos, comparar diferentes parâmetros de soldagem e prever efeitos térmicos no material. A aplicação facilita o entendimento do impacto de cada variável no resultado final, tornando o aprendizado e o controle industrial mais acessível. É recomendada para quem busca qualidade, segurança e eficiência em processos de soldagem, seja na prática industrial ou em estudos técnicos."
                           Style="{StaticResource BodyLabel}"
                           HorizontalTextAlignment="Center" />
                </VerticalStackLayout>
            </Frame>

            <Button Text="Usar"
                    Clicked="OnUsarClicked"
                    BackgroundColor="#2196F3"
                    TextColor="White"
                    FontAttributes="Bold"
                    CornerRadius="10"
                    Padding="10,8"
                    WidthRequest="150"
                    HorizontalOptions="Center"/>
        </VerticalStackLayout>

        <VerticalStackLayout Grid.Row="1"
                             Padding="20"
                             Spacing="10"
                             VerticalOptions="End"
                             HorizontalOptions="Center">

            <Label Text="Desenvolvido por Matheus de Andrade"
                   FontSize="14"
                   TextColor="{AppThemeBinding Light={StaticResource SecondaryTextLight},
                                                  Dark={StaticResource SecondaryTextDark}}"
                   HorizontalOptions="Center" />

            <HorizontalStackLayout Spacing="30"
                                   HorizontalOptions="Center">
                <ImageButton Source="linkedin.png"
                             Clicked="OnLinkedInClicked"
                             WidthRequest="32"
                             HeightRequest="32"
                             BackgroundColor="Transparent"/>
                <ImageButton Source="gmail.png"
                             Clicked="OnGmailClicked"
                             WidthRequest="32"
                             HeightRequest="32"
                             BackgroundColor="Transparent"/>
                <ImageButton Source="site.png"
                             Clicked="OnSiteClicked"
                             WidthRequest="32"
                             HeightRequest="32"
                             BackgroundColor="Transparent"/>
            </HorizontalStackLayout>
        </VerticalStackLayout>
    </Grid>
</ContentPage>
